# MinIO Cluster with Docker Compose

## Usage

1. Create a `.env` file based on the `.env.example` file to configure your MinIO cluster settings.
2. Run the following command to start the MinIO cluster:

   ```bash
   docker compose up -d
   ```
